
<div class="row">
    <div class="col">
        <a asp-action="Create">+ Novo</a>
    </div>
    <div id="filtro-escala" class="col" data-url="@Url.Action("QuadroPorEscala","Quadro")">
        <select class="form-control" onchange="SelectEscala(this.value)" >
            <option value="0">Selecione a escala/mês</option>
            @foreach (var itemEscala in ViewBag.Escala){
                <option value="@itemEscala.Id">@itemEscala.Mes</option>
            }            
        </select>
    </div>    
</div>


<div id="tabelaQuadro">
    @await Html.PartialAsync("_TabelaQuadro")
</div>


